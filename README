bin
===

Collection of general-purpose tiny scripts

# Usage

Create symlinks for each scripts to your $PATH or install plugin manager like `zplug` and add following line in your shell configutation file.

```
zplug "yutayamate/bin", as:command, use:"bin/*"
```
